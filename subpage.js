const SUPABASE_URL="https://mbigygpfxznlvcjfelvy.supabase.co";const SUPABASE_KEY="sb_publishable_hXo7cNYMBoPVxOJ_33gkkw_QjYt5e5u";const db=supabase.createClient(SUPABASE_URL,SUPABASE_KEY);const menu=document.querySelector(".menu-button");if(menu)menu.addEventListener("click",()=>{const open=document.body.classList.toggle("menu-open");menu.setAttribute("aria-expanded",String(open))});document.querySelectorAll(".desktop-nav a").forEach(a=>a.addEventListener("click",()=>document.body.classList.remove("menu-open")));async function load(){const nodes=[...document.querySelectorAll("[data-section]")];const sections=[...new Set(nodes.map(n=>n.dataset.section))];if(!sections.length)return;const status=document.querySelector(".load-status");const{data,error}=await db.from("site_content").select("section,title,content").in("section",sections);if(error){if(status){status.textContent="Using default content";status.classList.add("show");setTimeout(()=>status.classList.remove("show"),2200)}return}const rows=Object.fromEntries((data||[]).map(r=>[r.section,r]));nodes.forEach(node=>{const row=rows[node.dataset.section];if(!row)return;const field=node.dataset.field||"title";const value=row[field];if(value==null||value==="")return;if(node.dataset.attr==="href")node.href=value;else node.textContent=value})}load();